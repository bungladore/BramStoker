package mrflvhead.parts{	import flash.display.Sprite;	import flash.display.Bitmap;	import flash.events.Event;	import flash.display.Graphics;	import flash.text.TextFormat;	import flash.text.TextField;	import flash.text.TextFieldAutoSize;	import flash.display.Shape;		public class TimeCode extends Sprite	{				private var _time_code:Object;		private var _time_text:TextField;		private var _total_text:TextField;		private var _mask:Sprite;		private var _container:Sprite;		private var _default_format:Object;		private var _ct:Number;		private var _tc_str:String;		private var _additive_flavor1:String;		private var _additive_flavor2:String;		private var _additive_flavor3:String;				public function TimeCode(time_format:Object = null)		{			super();						_time_text = new TextField();            _time_text.autoSize = TextFieldAutoSize.RIGHT;            _time_text.background = false;            _time_text.border = false;			_total_text = new TextField();            _total_text.autoSize = TextFieldAutoSize.RIGHT;            _total_text.background = false;            _total_text.border = false;			var format:TextFormat = new TextFormat();            format.font = "Arial";            format.color = 0xFFFFFF;            format.size = 11;            _time_text.defaultTextFormat = format;			_total_text.defaultTextFormat = format;            addChild(_time_text);			addChild(_total_text);			_total_text.x = 153;		}				public function updateTime(current_time:Number):void		{			_time_text.text = HHMMSS(current_time);		}				public function updateTotal(total_time:Number):void		{			_total_text.text = HHMMSS(total_time);		}				private function HHMMSS(time:Number):String		{			var time:Number = Math.floor(time);						var hours:Number = Math.floor(time/3600);			var remains:Number = time%3600;			var minutes:Number = Math.floor(remains/60);			var seconds:Number = remains%60;						var hours_str:String = String(hours);			var minutes_str:String = String(minutes);			var seconds_str:String = String(seconds);						while(hours_str.length < 2){				hours_str = "0" + hours_str;			}						while(minutes_str.length < 2){				minutes_str = "0" + minutes_str;			}						while(seconds_str.length < 2){				seconds_str = "0" + seconds_str;			}						return hours_str + ":" + minutes_str + ":" + seconds_str;		}				private function makeDefaultFormat():void		{			                    }	}}